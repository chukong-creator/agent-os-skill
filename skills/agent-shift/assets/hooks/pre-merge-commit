#!/bin/sh
if [ "${AGENT_SHIFT_ALLOW_MAIN_COMMIT:-0}" != "1" ]; then
  echo "Agent Shift: direct merges into protected main are blocked." >&2
  echo "Run the merge gate and use agent-shift merge." >&2
  exit 1
fi
