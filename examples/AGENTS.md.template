# Project context

Briefly state what this repository is for and who uses the delivered result.
Do not repeat general coding advice or the Agent OS protocol here.

## Repository gotchas

- Replace this line with a surprising convention that the repository alone does
  not make obvious and that would fail silently if ignored.
- Replace this line with the authoritative location for generated files, shared
  types, migrations, fixtures, or another project-specific constraint.

## Verification routing

- Name the fastest relevant project check and the final delivered-form check.
- Declare the architecture and knowledge sources audited by `knowledge-doctor`.
- Put long verification, release, migration, or design procedures in a Skill or
  referenced file and link it here only when it applies.

## Governance routing

- Ordinary local, reversible work uses the direct inspect → edit → verify loop.
- When cross-Agent handoff, isolation, recovery, independent acceptance, or an
  external release needs governance, read the installed `agent-os` Skill.
- Keep permissions, exact-commit Evidence, Review, Merge Gate, and rollback in
  Agent OS; do not duplicate that protocol in this file.
