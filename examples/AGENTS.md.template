# Project collaboration rules

## Roles

- Codex owns goals, product and architecture decisions, Work Package scope, risk, review, and final acceptance.
- Claude Code owns authorized implementation and ordinary review rework in its assigned Agent worktree.
- Independent reviewers inspect evidence and findings; they do not approve their own implementation.

## Git and delivery

- Do not start Agent OS for a local, reversible change that one Agent can complete and verify in the current task. Use it only when handoff, isolation, recovery, independent acceptance, or an external release materially needs governance.
- Keep the protected base branch clean and recorded as an Agent Shift baseline before delegation.
- Choose proportional governance: `L0` for low-risk governed work, `L1` for ordinary measurable delivery or a reversible authorized release, and `L2` for production, privacy, credentials, migrations, deletion, payment, irreversible, or other material external work.
- Give every write-capable Agent an isolated branch and worktree. Never allow two writers in one worktree.
- Commit approved Work Packages and governance before starting a Run.
- Merge only the exact Agent commit that passed verification, independent review, and the merge gate.
- For `L1` and `L2`, freeze the outcome metric, baseline, target, validation window, and evidence source before implementation; record the observed result after merge.
- For `L2`, require an independent Director Challenge plus the five-question maturity gate.

## Permissions and evidence

- Work only inside the Work Package allowlist and declared repository root.
- Do not push, deploy, publish, access credentials, or perform irreversible actions without explicit authority.
- Record commands, exit status, observed results, exact commit, changed paths, and residual risks.
- For a live release, hash the final publishable artifact and pass real checks against the exact endpoint. Any repackaging invalidates earlier evidence.
- Classify claims as verified, reviewed, observed, or assumed; never promote an assumption into evidence.
- Record governance economics honestly. Leave token usage unknown when the runtime does not expose trusted usage.

## Project-specific verification

- Replace this section with the real build, test, lint, type-check, render, or release commands for this project.
